import { createSlice } from '@reduxjs/toolkit';

const pickSlice = createSlice({
  name: 'pick',
  initialState: {},
  reducers: {},
});

// export const {
//     chooseOption,
// } = pickSlice.actions;

export default pickSlice.reducer;
